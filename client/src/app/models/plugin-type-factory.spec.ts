import { PluginTypeFactory } from './plugin-type-factory';

describe('PluginTypeFactory', () => {
  it('should create an instance', () => {
    expect(new PluginTypeFactory()).toBeTruthy();
  });
});
