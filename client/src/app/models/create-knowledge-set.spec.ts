import { CreateKnowledgeSet } from './create-knowledge-set';

describe('CreateKnowledgeSet', () => {
  it('should create an instance', () => {
    expect(new CreateKnowledgeSet()).toBeTruthy();
  });
});
